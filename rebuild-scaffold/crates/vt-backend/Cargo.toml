[package]
name = "vt-backend"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
backend-contract = { path = "../backend-contract" }
bitstream-core = { path = "../bitstream-core" }
